# Data Instructions

This project uses Yahoo Finance to fetch data automatically. No manual download is required.

If you prefer to use a local CSV, place it in this folder and modify `data_loader.py` to load it.
